# Define a shared memory zone 'one' of 10MB to track clients by IP efficiently
# Limit each client IP to a request rate of no more than 1 request per second
limit_req_zone $binary_remote_addr zone=one:10m rate=1r/s;
